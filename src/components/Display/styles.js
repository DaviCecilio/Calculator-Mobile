import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  display: {
    flex: 1,
    padding: 20,
    justifyContent: 'center',
    alignItems: 'flex-end',
  },

  displayValue: {
    fontSize: 60,
    fontWeight: 'bold',
  },
})
